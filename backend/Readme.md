### Run Locally

```bash
# git clone
cd backend
# create and addd env variables
vim .env
# add variables
DB_USER=
DB_PASSWORD=
DB_URL=
DB_NAME=
```

```bash
docker build -t back .
docker run -p 80:80
```
